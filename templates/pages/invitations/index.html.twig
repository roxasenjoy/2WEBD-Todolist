{% extends "/layouts/layout.html.twig" %}

{% block content %}
    <h1>Invitations index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Accepted_invitation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for invitation in invitations %}
            <tr>
                <td>{{ invitation.id }}</td>
                <td>{{ invitation.acceptedInvitation ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('invitations_show', {'id': invitation.id}) }}">show</a>
                    <a href="{{ path('invitations_edit', {'id': invitation.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('invitations_new') }}">Create new</a>
{% endblock %}
